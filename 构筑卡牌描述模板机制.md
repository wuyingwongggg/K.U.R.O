# 构筑卡牌描述模板机制（BuildCard 层级高亮）

## 概述

卡牌 `BuildEffectDefinition.Description` 支持模板占位符，选卡界面渲染时从卡牌的 `PropertyOverrides` 按名读取层级数值，填充进描述并**高亮当前可获得层**（金色），其余层暗灰显示。

```
模板: {数组名:下标}    例: {GainValues:0}   简写: {0} ≡ {TierValues:0}
```

## 数据链路

```
模板 token {GainValues:0}
  → BuildSelectionWindow.BuildTierDescription()（正则 {([A-Za-z_][A-Za-z0-9_]*)?:?(\d+)}）
  → BuildEffectDefinition.GetOverrideFloatArray("GainValues")
      → GetEffectOverrides() → EffectEntries[0].PropertyOverrides    ← 只取第一个 entry
      → TryGetValue("GainValues") 按字符串键名查字典
      → Variant.Obj 解包成 float[]（Godot.Array 或 IEnumerable 两种分支）
  → values[0] → BBCode 高亮填充
```

当前层索引 = 玩家已获得层数 `stacks`（来自 `BuildSelectionManager._pickedEffectIds`），**stacks=0 高亮第一个值**（本次选中后将获得第 1 层）。

## 需要注意的点

### 1. 键名大小写敏感
模板里的数组名必须与 `.tres` 中 `PropertyOverrides` 的键名**逐字符一致**。`{gainvalues:0}` 找不到 `"GainValues"`，会保留原文不替换。

### 2. 只读第一个 EffectEntry
`GetEffectOverrides()` 固定取 `EffectEntries[0]`。多 entry 卡牌（如 A_008 主效果 + BurnEntry）的**层级数值必须放在第一个 entry 的 PropertyOverrides** 里。

### 3. 显示值 = 配置值，不是描述文本
模板化后卡面数字来自 `PropertyOverrides`。改数值只需改配置，但**描述里残留的硬编码数字会与配置不一致**（A_007 曾出现"描述写 75/100、配置写 100/200"的错位）——模板化时务必核对两侧。

### 4. 负值按绝对值显示
修改器百分比为负（减容/缓速类：`[-30, -50]`）时，渲染取 `Mathf.Abs`，描述显示"降低 30%"而非"降低 -30%"。

### 5. 占位符可多处复用
同一 token 在描述中出现多次全部替换（A_006 的 `{0}%/{1}%` 出现 3 次），且高亮同步。

### 6. 多数组共享同一层索引
一张卡可有多个数组（A_007：GainValues/CapValues/DecayValues），**所有数组的高亮索引都是同一个 stacks**，不分别计数。

### 7. 下标越界 / 找不到数组 → 保留原文
token 无法解析时原样输出，不影响描述其他部分，也不报错。stacks 超出数组长度时 clamp 到最后一项。

### 8. 无层级数据的卡不受影响
`TierValues` 或其他数组缺失、描述无 `{` 时，整条描述按纯文本显示——其他流派/固定数值卡无需改动。

### 9. 渲染层依赖
`DescLabel` 必须是 **RichTextLabel**（`bbcode_enabled = true`）。普通 `Label` 无法局部着色。卡面缩放（`ApplyCardScale`）对 RichTextLabel 的字号覆盖同样生效。

### 10. .tres 数组两种写法均可
`"TierValues": [10.0, 20.0, 30.0]` 与 `"TierValues": Array[float]([10.0, 20.0, 30.0])` 加载后都会被 Variant 包装，代码有 `Godot.Collections.Array` / `IEnumerable` 两个分支，写法不影响。

### 11. 浮点显示
整数值自动去掉小数（50.0 → "50"）。

### 12. 满层卡不会出现在选卡中
`BuildSelectionManager` 已按 `MaxStacks` 排除满层卡，因此卡面上不会出现"全部层都高亮"的情况。

## 已模板化的卡

| 卡 | 数组 | 模板示例 |
|----|------|---------|
| A_001~A_005、B_001~B_003 | TierValues | `{0}%/{1}%/{2}%` |
| A_006 | TierValues（复用 3 次） | `低于{0}%/{1}%…恢复{0}%/{1}%` |
| A_007 | GainValues/CapValues/DecayValues | `{GainValues:0}%…{CapValues:0}%…{DecayValues:0}%` |
| A_008 | TierValues | `突破当前的{0}%/{1}%` |

未模板化（无层级数值）：A_009、A_010、B_004~B_010。

## 新卡接入步骤

1. 效果脚本定义数组导出字段（`[Export] float[] TierValues` 或独立命名数组）
2. 卡牌 `.tres` 的 PropertyOverrides 填入数组（放**第一个** EffectEntry）
3. Description 写模板（数值位用 `{数组名:下标}`，单位留在模板外）
4. 无需改代码，选卡界面自动高亮
